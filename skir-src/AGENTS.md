# Contract (Skir)

Generated files live in `server/src/skir/skirout/` and `client-web/src/data/skirout/`. **Never edit them directly** — change `*.skir` files here then run `just skir-gen`.

Breaking contract changes must include backend driver and frontend adaptations in the **same PR**. Verify with `just skir-check`.
